@model IEnumerable<VitalFlow.Models.Zaliha>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.krvnaGrupa)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.kolicina)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.kriticnaLinija)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.krvnaGrupa)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.kolicina)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.kriticnaLinija)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.hubID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.hubID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.hubID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
