@model IEnumerable<VitalFlow.Models.HUB>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.terminID)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.zahtjevID)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.terminID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.zahtjevID)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.hubID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.hubID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.hubID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
